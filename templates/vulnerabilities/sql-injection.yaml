id: sql-injection

info:
  name: SQL Injection Vulnerability
  author: khushi-nirsang
  severity: critical
  description: Detects possible SQL Injection through error-based responses, time delays, or behavioral analysis.
  tags:
    - sql-injection
    - injection
    - a03:2021
    - a03:2017

requests:
  - method: GET

    path:
      - "{{BaseURL}}/?id={{payload}}"
      - "{{BaseURL}}/?search={{payload}}"
      - "{{BaseURL}}/?q={{payload}}"
      - "{{BaseURL}}/?user={{payload}}"
      - "{{BaseURL}}/?product={{payload}}"
      - "{{BaseURL}}/search?query={{payload}}"
      - "{{BaseURL}}/product?id={{payload}}"
      - "{{BaseURL}}/api/search?q={{payload}}"

    payloads:
      - "payloads/sqli/error-based-sql-injection-payloads.txt"
      - "payloads/sqli/sql-injection-all-payloads.txt"

    matchers:

      - type: regex
        part: body
        regex:
          - "(?i)sql syntax"
          - "(?i)you have an error in your sql syntax"
          - "(?i)warning.*mysql"
          - "(?i)mysql_fetch"
          - "(?i)mysql_num_rows"
          - "(?i)mysqli"
          - "(?i)postgresql"
          - "(?i)pg_query"
          - "(?i)sqlite"
          - "(?i)sqlite_exception"
          - "(?i)ora-[0-9]+"
          - "(?i)microsoft ole db"
          - "(?i)unclosed quotation mark"
          - "(?i)sqlserver"
          - "(?i)odbc"
          - "(?i)jdbc"
          - "(?i)database error"
          - "(?i)column.*not.*found"
          - "(?i)table.*not.*found"

    matchers-condition: or

  - method: GET
    
    path:
      - "{{BaseURL}}/?id={{payload}}"
      - "{{BaseURL}}/?search={{payload}}"
      - "{{BaseURL}}/search?query={{payload}}"

    payloads:
      - "payloads/sqli/union-select-sql-injection-payloads.txt"

    matchers:
      - type: word
        part: body
        words:
          - "union"
          - "select"
        condition: and

      - type: status
        status:
          - 200

    matchers-condition: and
